{
  "name": "mcp-internal-developer-platform-demo",
  "version": "1.0.0",
  "description": "A self-contained TypeScript demo of MCP tools for internal development platform.",
  "main": "src/server.ts",
  "type": "module",
  "scripts": {
    "build": "tsc && node scripts/fix-extensions.cjs",
    "start": "npm run build && node dist/server.js",
    "test": "vitest run"
  },
  "devDependencies": {
    "typescript": "^5.0.0",
    "zod": "^3.22.0",
    "vitest": "^0.34.0",
    "ts-node": "^10.9.0"
  }
}
